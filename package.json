{
  "name": "arnold-academy-park",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "test": "vitest run",
    "lint": "eslint src tests scripts",
    "lint:css": "stylelint \"pages/**/*.css\" \"styles/**/*.css\"",
    "check": "npm run lint && npm run lint:css && npm run test && npm run build"
  },
  "devDependencies": {
    "@eslint/js": "^9.9.0",
    "eslint": "^9.9.0",
    "stylelint": "^16.8.0",
    "stylelint-config-standard": "^36.0.1",
    "vitest": "^2.0.5"
  }
}

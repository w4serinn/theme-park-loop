# ROADMAP

## 世界観(確定)

- パーク名: 王立魔法学院アルノルド
- コンセプト: 蒸気機械と魔法が共存する、全寮制の魔導学院
- コアカラー(不変): `--brass`(真鍮) / `--emerald`(深緑) / `--ink`(深紺) / `--ember`(アンバー)
- アトラクション名の例(あくまで着想の参考。エリア追加タスクでは、この3つに限らず
  世界観に沿った新しい名前を考えること): 時計塔の錬金術ラボ / 飛行船ドック /
  魔法陣召喚エレベーター
- ページアクセント色は `docs/PALETTE.md` の承認制。新規提案は実装に使わず、
  次回人間の承認を待つ。

## ページ制作の品質方針

evolveループはこの方針を毎サイクル遵守すること:

- **アトラクション数**: エリアの規模・性格に合わせて自由に設定する(2〜7件程度)。
  全エリアで同じ件数にならないよう、意図的にバラつかせること。
- **テキスト**: 各項目の文章量・視点・トーンを変え「コピーした感じ」を避ける。
  固有名詞・具体的な数字・エピソードを含めて世界観を作り込む。
- **レイアウト多様化**: セクションを作るたびにカードグリッドで解決しない。
  prose+aside・タイムライン・dl定義リスト・横スクロール・大きな単一ビジュアル等、
  内容に合った形を先に検討してから実装すること。カードは「複数の同格アイテムを
  並べる」ときだけ選ぶ。
- **エフェクトの意味**: hover・transition・animation には必ず目的を持たせる。
  「クリックできる」「状態が変わる」「注目してほしい」のどれかを伝えるために使う。
  リンクでないカード・装飾的な要素への `translateY` や border-color 変化は使わない。
  リンクカードには arrow の移動・border 明度変化など「行き先がある」ことを示す
  表現を優先する。
  **ただし目的を満たしているだけでは不十分**(2026-07-27 `worldview-check` 指摘で
  判明): 以下は「目的はあるが量産型」になりがちなので、代わりに蒸気機械系
  (歯車の回転、蒸気/煙のパーティクル、機械的に急停止するイージング、圧力メーター的
  な変化)または魔法系(魔法陣のstroke描画、淡い発光の強弱、展開/収束する動き)の
  質感に置き換えられないか必ず検討すること。目的だけ満たして下記のまま実装した
  場合、`worldview-check` スキルで「要修正」判定される前提で作業する。
  - `transform: scale(...)` によるただの拡大 → 発光の強弱や歯車の回転などに置換
  - 汎用的な box-shadow の拡大(「浮き上がり」表現) → 発光(text-shadow等)に置換
  - フェードだけ(`opacity` の変化のみ、または `ease-out` 等の汎用イージングのみ)の
    画面遷移・出現演出 → 機械式の急停止イージングや魔法陣の展開等に置換
- **素材の積極依頼**: `docs/ASSET_REQUESTS.md` への素材依頼は hero 画像だけに限らない。
  セクション背景テクスチャ・エリアアイコン・インライン挿絵・カードサムネイルなど、
  「小さいイラストでも世界観が明確に上がる」と判断したら即座に依頼すること。
  ユーザーは依頼があれば自分で作ると言っている。躊躇せず提案する。
- **色・素材の提案**: PALETTE.md への色提案と ASSET_REQUESTS.md への素材依頼は、
  「あると体験が明確に良くなる」と思ったら積極的に行うこと。1サイクル1提案が目安。
- **テキストコントラスト**: テキスト色は `var(--brass)` または
  `color-mix(in oklch, var(--brass) 75%以上, transparent)` を使う。
  `var(--ink)` と混ぜると暗くなるため、文字色に `var(--ink)` を混ぜてはならない。
  `var(--emerald)` は暗い深緑なので、dark背景上のテキスト色に使ってはならない。
- **完了の基準を上げる**: アトラクション追加・インタラクション・画像対応・
  テキスト充実が揃って初めて`完了`とする。骨格を作っただけで`完了`にしない。

## バグ修正(最優先 — 通常機能より先に上から順に着手する)

- [ ] (S) 中庭の大魔法陣の直径表記が不一致(2026-07-28、ARG語彙棚卸し中に発見)。
      `pages/index.html`(学院の3大体験セクション)では「直径20mの大魔法陣」、
      `pages/exploration/summoning-plaza.html`では「大魔法陣『刻の輪』…直径30mの
      中央魔法陣本体」と表記が異なる。どちらかに統一する(summoning-plaza.html側の
      方が詳細な固有名詞付きの記述のため、index.html側を30mに合わせるのが
      自然と思われるが、実装時に前後の文脈も確認して判断すること)。

## 新規ページ提案(承認待ち — 承認されたら下の「ページ一覧」に移動する)

新しい独立ページの追加提案、および既存の一覧系ページで3階層目のページが
必要だと判断した場合の提案は、どちらもここに書くこと。実装には進まない。

(現在なし)

---

## ページ一覧と進行状況

各ページは S/M サブタスクに分割し、数サイクルかけて完成させる。
一覧系ページ(学院内探索・購買部・学食)は「一覧 → エリア/カテゴリページ」の
2階層までとする。エリア・カテゴリの数はコアで固定せず、自由に増やしてよい。
ページ自体の追加は上記「新規ページ提案」を経由する。

あるページのstatusを`完了`に変える際は、見出し直後に世界観に沿った紹介文を
1〜2文で追記すること: `> 紹介文: <本文>`。この文章がトップページの更新履歴に
そのまま表示される(自動生成の詳細はevolve/SKILL.md参照)。

### 0. 共通パーツ(ヘッダー/フッター) [status: 完了]

> 紹介文: ヘッダーの「入学願書」ナビリンクが、どのページからでも一目でわかる
強調ボタンになりました。気になったときにすぐ入学願書ページへ進めます。

(現在、追加のサブタスクなし。今後も見た目の粗さや`worldview-check`の指摘が
あれば随時ここに追記する)

### 1. トップページ [status: 完了]

> 紹介文: 王立魔法学院アルノルドの正門が開かれました。学院の歴史・体験・アクセスまで、来訪の決め手となる情報が一堂に揃っています。

既存: ヒーローエリア・ニュースセクション完成済み。
目標: 初めて訪れる人が「来訪を決意できる」ページ。情報・世界観・導線の全てを揃える。

### 2. 学院内探索 [status: 完了]

> 紹介文: 学院内探索の7エリアすべてに、そのエリアならではの設備紹介セクションが加わりました。永久運動核が時を刻む時計塔から、契約精霊が棲む魔法陣召喚広場まで、各エリアの個性がより深く味わえるようになっています。

### 3. 学院祭・行事 [status: 完了]

> 紹介文: 一年の魔法暦を彩る8つの行事が、さらに深く楽しめるようになりました。各行事に「豆知識」を追加したほか、季節タグに加えてエリアタグも新設し、季節×エリアの複合フィルタリングで気になる行事を絞り込めます。

### 4. 購買部 [status: 進行中] (一覧→カテゴリ の2階層)

> 紹介文: 購買部が全面開店しました。中央購買部の制服・魔導具・書籍・食料品・みやげに加え、7エリアそれぞれに専門店が軒を連ねます。錬金術研究棟の蒸留工房店から天文台の夜空雑貨店まで、全14の個性豊かな店舗を巡ってみてください。

注: shop/index.html をエリア別タブ構造に改修し、各エリアに複数の店舗詳細ページを追加する。
詳細ページはすべて pages/shop/*.html (depth-1) に置くため2階層ルールは維持される。
ユーザーが「階層ルールを破るとしても許可する」と明示的に述べているので、
local-review が2階層違反を指摘した場合も、このタスク群については対応不要として扱うこと。

- [ ] (S) 商品アイコン(product-entry__visual)の残り10種を反映(REQ-058〜067、
      素材到着待ち)。13種中3種(制服・魔導具・みやげ)は反映済み。

### 5. 学食・喫茶室 [status: 完了]

> 紹介文: 学食・喫茶室が全エリアで開店しました。中央学食の定番メニューから各エリア固有の飲食店まで、魔法効果つき料理と個性豊かなドリンクが揃います。秘薬スタンドのフラスコドリンク、歯車喫茶のスチームミルクティー、星見ダイナーの夜景ディナーなど、エリアタブでお気に入りの一軒を見つけてください。各エリアタブに人気メニューのプレビューも加わり、ひと目で店の魅力が伝わるようになりました。

注: dining/index.html をエリア別タブ構造に改修し、各エリアの飲食店詳細ページを追加する。
詳細ページはすべて pages/dining/*.html (depth-1) に置くため2階層ルールは維持される。
ユーザー明示許可あり(shop と同様)。

### 6. 入学願書(チケット案内) [status: 完了]

> 紹介文: 入学願書受付所がさらに充実しました。大人・学生・小人・家族の料金一覧と自動計算シミュレーターに加え、四季を通じて通える年間パス「四季来訪証」、団体・修学旅行向けプラン、チケットに関するよくある質問まで、来場前の疑問にすべてお応えします。

### 7. 学院への道のり(アクセス+マップ) [status: 完了]

> 紹介文: 学院への道案内がさらに充実しました。電車・バス・お車の交通手段一覧、8エリアを収めたインタラクティブなキャンパスマップに加え、初めての方向けモデルルート・駐車場の詳細情報・周辺の提携宿泊施設まで、来訪計画に必要な情報がすべて揃いました。

### 8. 学院案内(サービス情報) [status: 完了]

> 紹介文: 学院案内所がさらに充実しました。服装・持ち物の3区分パネル、6問のQ&Aアコーディオン、5つのルール解説に加え、園内サービス施設・バリアフリー多言語対応の案内、そして学院内を案内してくれる人懐っこい魔法生物たちの紹介コーナーも新設しました。

### 9. OGPアイキャッチ画像 [status: 完了]

> 紹介文: 王立魔法学院アルノルドの外観を一望できるアイキャッチ画像が完成しました。SNS等でサイトのURLを共有すると、時計塔と飛行船が浮かぶ夕景のプレビューが表示されます。

サイト全体で使う共通アイキャッチ画像(og:image)を主要8ページに設定する取り組み。
og:type/og:title/og:descriptionは全ページ設定済み。

### 10. ページ内検索機能 [status: 完了]

> 紹介文: サイト内検索が開設されました。学院内探索・購買部・学食など、全41ページをキーワードで横断検索できます。ヘッダーの「検索」から、気になるエリアや店舗をすぐに見つけられます。

サイト全体(全41ページ)を横断検索できる pages/search.html を新設。検索対象データは
src/search-data.js に静的な配列として定義し(file:// 環境でも動作するよう
fetch/JSONは使わずグローバル変数として読み込む)、タイトル・カテゴリの部分一致で
絞り込む。フィルタリングの純粋関数 filterSearchIndex は src/logic.js に実装し
テスト済み。ヘッダーナビに「検索」リンクを追加。

### 11. 更新履歴の全件表示 [status: 完了]

> 紹介文: トップページの更新履歴が、過去のお知らせも含めてすべて閲覧できるようになりました。一覧はスクロール式の小窓に収まっているので、ページが間延びすることなく学院の歩みを振り返れます。

トップページの更新履歴(`<!-- NEWS -->` → `data/news.json`)が直近10件で打ち切られ、
それより古いお知らせが閲覧不能になっていた問題を解消。`scripts/news-render.js` に
`renderNewsListHtml()` を切り出し件数上限を撤廃、全件を出力するようにした上で、
`.news-list` を `.news-list-scroll`(`max-height` + `overflow-y: auto`)で包み、
スクロール可能であることを示す下端フェードを追加した(横スライドではなく縦スクロール
を採用。カード列ではなく1カラムのリストのため、横スライドよりスクロールの方が
自然に読めると判断)。

### 12. 全ページ演出・体験強化 [status: 進行中]

「今後のタスク候補」から着手(2026-07-27 ユーザーフィードバック起点)。現状、動きの
演出があるのはトップページのヒーロー到着フェード(`hero-fade-up`)と「学院の3大体験」
カルーセルのみで、他ページには `@keyframes` が一切無いことをコード調査で確認済み
(`styles/*.css` 横断で `@keyframes` は index.css にしか存在しない)。サイズLのため、
このサイクルではページ単位・要素単位のS/Mサブタスクへの分割のみを行い、実装は次回
以降のサイクルで1項目ずつ着手する。各項目とも「ページ制作の品質方針」のエフェクトの
意味の原則(クリックできる/状態が変わる/注目してほしい、のいずれかを伝える)と
`prefers-reduced-motion` 対応(index.cssの既存実装を踏襲)を守ること。

**方向性の修正(2026-07-27 ユーザーフィードバック)**: トップページのサブタスクを
実装したところ、(1)歯車の装飾が0.9rem/16s周期と控えめすぎて視認できない、
(2)結局スクロールfade-inとホバーの変化しか実装していない、と指摘された。
「エフェクトの意味」原則を守ることを意識しすぎて、視覚的な存在感が乏しい・
バリエーションが単調という逆方向の失敗をした。歯車は1.8rem/9s周期+光彩付きに
修正済み(about-section・ticket-info-section)。ユーザーに残りサブタスクの優先方向を
確認したところ、**インタラクション性(クリック/状態変化で反応する仕掛け)** と
**イラスト・キャラクターの動き** を優先する方針が確定した。以降のサブタスクは、
単純なfade-in/hoverの繰り返しで終わらせず、上記いずれかの要素を必ず含めること。
ナレーション・音は今回優先度から外れたため最後に据え置く。

- [ ] (S) ナレーション音声の実装(REQ-070〜072、素材到着待ち): 音声ファイルが
      届いたら、該当ページ(トップページ・学院内探索・入学願書)に控えめな
      再生ボタンを設置して紐付ける(自動再生はしない)。

### 13. 隠し用語集ページ群 [status: 進行中]

(履歴: 2026-07-27に`pages/glossary/mythical-creatures.html`を承認、単発ページから
複数の隠しページを対象とする取り組みに拡張。2026-07-28、ユーザーとの設計対話を経て
ARG(代替現実ゲーム)的な「発見の連鎖+謎解き+最終ゴール」構造に全面拡張した。
**以降の作業は`docs/ARG-DESIGN.md`を正とする。このセクションは概要のみ。**)

概要・仕組み・実装手順はすべて`docs/ARG-DESIGN.md`を参照(このファイルには
重複して書かない)。既存の4ページ(魔法生物図鑑・永久運動術式・星図の断片・
決闘王列伝、いずれも実装済み)は同ファイルのP1〜P4としてこの構造に組み込まれている。

**承認について(2026-07-28)**: `docs/ARG-DESIGN.md`はユーザーと対話しながら
共同設計したものであり、そこに載っているP5以降・PGATE・PFINAL(今後追加される
フレーバー層の行を含む)は実装のたびに個別承認を取る必要はない(事前承認済み)。
設計図の追記ルールの外で新しいページを思いついた場合のみ、通常どおり
「新規ページ提案」セクションを経由して承認を取ること。

`### 15`(ARG基盤・ノスティオン)の土台整備が終わり次第、P番号の隠しページを
`docs/ARG-DESIGN.md`4節の表から選んで実装していく(実装時の共通ルールは同ファイル
5節参照)。それまでは`### 15`を優先する。

- [ ] (S) `docs/ARG-DESIGN.md`4節の表にある「未着手」のP番号ページを1行(または
      関連する2〜3行)選んで実装する(`### 15`の土台整備完了後に着手)。
      実装するたびに同ファイルの該当行のstatusを更新する。表には常に「未着手」の
      行が残り続ける想定(フレーバー層は上限を設けず今後も増える、
      `docs/ARG-DESIGN.md`4節参照)のため、このタスク自体は他のP番号タスクと
      同様チェックを付けず、毎サイクル同じ行として選び直してよい。

**初期42ページの語彙棚卸し(2026-07-28 ユーザー指示)**: 4-2節「通常ページ発の
新規ルート群」のroot行(P10・P12・P16・P18・P20等)が「通常ページ(未定)」の
プレースホルダーのままで、実際にどの語句を使うか未選定だった。実際に読み直すと
学院内探索・購買部・学食(特に複数ページを跨ぐ「月草」「学院農園」等の共通語彙)
に想像以上の候補が眠っていることが判明したため、42ページ全てを8分類で棚卸しし、
`docs/ARG-WORDBANK.md`に候補を書き出す。**このタスクの成果はARG-DESIGN.md本体
ではなく`docs/ARG-WORDBANK.md`(候補置き場、正式採用時にARG-DESIGN.mdへ転記)に
書くこと。** すでに部分的に着手済み(`docs/ARG-WORDBANK.md`参照、各分類のstatus欄
に収集状況を記載)。

**再棚卸しについて(2026-07-28 ユーザー指摘)**: 学院内探索・学院祭・購買部の
3分類は一度棚卸しを行ったが、学院祭ではリサーチエージェントによる最初の
一次収集が「豆知識」欄の記述(占術師17代目等)を見落としており、後から
手動で読み直して発見した経緯がある。他の分類でも同様の見落としがある
可能性が高いため、**この3分類も含めて全8分類を「一度は候補が出ているが
再確認が必要」として扱い、やり直す**。既に見つかっている候補は
`docs/ARG-WORDBANK.md`にそのまま残し、再棚卸しでは追加の発見のみを
書き足せばよい(ゼロからやり直す必要はない)。

- [ ] (S) 学院内探索(`exploration/`、8ページ)の再棚卸し。`docs/ARG-WORDBANK.md`
      「2.」に15件記載済みだが、他の分類と同様の見落としがないか読み直して
      確認・追記する。
- [ ] (S) 学院祭・行事(`events/index.html`)の再棚卸し。`docs/ARG-WORDBANK.md`
      「3.」に7件記載済みだが、一次収集がリサーチエージェント経由で見落とし
      (占術師17代目等)があった経緯があるため、再度読み直して確認・追記する。
- [ ] (S) 購買部(`shop/`、17ページ)の再棚卸し。`docs/ARG-WORDBANK.md`「4.」に
      10件記載済みだが、他の分類と同様の見落としがないか読み直して確認・追記する。
- [ ] (M) 上記8分類の棚卸しが出揃った段階で、`docs/ARG-WORDBANK.md`の候補群を
      横断的に見直し、**「同一ページにまとめられそうな候補」をグルーピングする**
      作業を行う(2026-07-28 ユーザー指示)。1ページあたり2〜3項目という新方針
      (本節末尾の「1ページあたりの情報量」参照)に沿って、以下のような組み合わせ
      の可能性を検討する:
      - 命名パターンが呼応するもの(例: 広場「刻の輪」×時計塔「刻の書」「刻の声」)
      - 同じモチーフの反復(例: 「魔法陣刻印記録簿」「陣紋補修記録簿」「管理台帳
        『刻の書』」「錬金術研究棟の記録簿」の"記録簿"4件)
      - 複数ページに跨る共通語彙(例: 「月草」6ページ、「学院農園」5ページ)
      グルーピング結果は`docs/ARG-WORDBANK.md`に追記する(各候補の末尾に
      「→ 他候補と統合可: ○○」のような形で書き足す想定)。8分類が全て完了する
      前でも、明らかに良い組み合わせに気づいた時点で先にメモしてよい。

**既存ARGページの作り直し(2026-07-28 ユーザー指摘・承認済み)**: P5〜P8・P91の
実装済み5ページが、(1)1ページあたりの項目数が1つのみでP1〜P4([2〜5項目]
より薄い、(2)fragment種別のページ(F1・F2・F13)が実際の謎解きを経ず単純訪問
だけで断片を獲得できてしまっている、という2点の指摘を受けた。今後の新規P番号
ページには`docs/ARG-DESIGN.md`5節に追記した新方針(項目数2〜3個・謎解き必須)を
適用するのに加え、以下の既存5ページも同方針に合わせて作り直す。新規ページ実装
より優先度は高くない(後から追加された振り返りタスクのため)が、`docs/ARG-DESIGN.md`
4節の「未着手」P番号タスクより先に着手してよい。

- [ ] (M) P5「見習い整備士の手記」(`pages/glossary/apprentice-notes.html`)を
      作り直す: 関連する項目を2〜3個程度に増やし、断片F1(刻の断片)の獲得を
      単純訪問ではなく実際の謎解きアクション(暗号解読・検索窓への合言葉入力・
      選択肢からの正解選択等、具体的な形式は実装時に検討)経由に変更する。
- [ ] (M) P6「初代天文官 シベル・オーレン」(`pages/glossary/first-astronomer.html`)
      を作り直す: 関連する項目を2〜3個程度に増やす(断片を産出しないページの
      ため謎解き対応は不要)。
- [ ] (M) P7「記録帳、最後の頁」(`pages/glossary/final-entry.html`)を作り直す:
      関連する項目を2〜3個程度に増やし、断片F2(記帳の断片)の獲得を実際の
      謎解きアクション経由に変更する。
- [ ] (M) P8「名を消された決闘王」(`pages/glossary/erased-champion.html`)を
      作り直す: 関連する項目を2〜3個程度に増やす(断片を産出しないページの
      ため謎解き対応は不要)。
- [ ] (M) P91「最初の記憶」(`pages/glossary/nostion-memory.html`)を作り直す:
      関連する項目を2〜3個程度に増やし、断片F13(本心の断片)の獲得を実際の
      謎解きアクション経由に変更する。ノスティオン自身への自己言及という
      P91固有の入口(検索欄への「私」「ノスティオン」の入力)は変更しない。

### 14. 提携宿泊施設 [status: 完了]

> 紹介文: 提携宿泊施設のご案内が充実しました。学院前旅籠「銀時計亭」・魔法街道ステーションホテル・郊外リゾート「星降る丘コテージ」それぞれの客室タイプ・設備・ご予約方法まで、専用ページで詳しくご覧いただけます。

(2026-07-28 ユーザー承認。「学院への道のり」ページの「周辺の提携宿泊施設」
セクションが3件のカード(名称・距離・一行説明・提携特典のみ)に留まっており、
部屋タイプ・設備・写真・予約方法などの詳細が無い状態だったのを受けて、
専用ページの追加を提案・承認)

概要: `pages/access/lodging.html`(depth-1)を新設し、学院前旅籠「銀時計亭」・
魔法街道ステーションホテル・郊外リゾート「星降る丘コテージ」の3施設それぞれに
詳細セクション(客室タイプ・設備・アクセス・提携特典の詳細・予約導線)を設ける。
`access/index.html`の既存カードグリッドはサマリーとして残し、「詳細を見る」
リンクで新ページへ誘導する形にする(カード自体を置き換えない)。
ヘッダーナビには追加しない(既に8項目+検索で飽和気味のため)。`pages/sitemap.html`
には通常ページとして掲載する(### 13の隠しページ群とは異なり、意図的に発見しやすい
位置に置く)。一覧→詳細の2階層ルールの対象は「学院内探索・購買部・学食」の3ページに
限定されているが、本ページは新規の単独ページ(1ページのみ)であり一覧ページの新設
ではないため、階層ルールの対象外。

### 15. ARG基盤・ノスティオン [status: 完了]

> 紹介文: 学院の検索窓口が一新されました。物知りの魔導書「ノスティオン」に、気になる言葉を何でも尋ねてみてください。学院内の出来事を、驚くほど詳しく教えてくれます。

`### 13`(隠し用語集ページ群、ARG化)を支える土台部分。隠しページ本体(P番号)は
`### 13`側で扱い、こちらは検索の仕組みと「物知りの魔導書『ノスティオン』」
(現行の検索ページの改名)そのものを対象にする。詳細仕様はすべて
`docs/ARG-DESIGN.md`(2節・4-7節)を参照。

（2026-07-28 ユーザー指示によりキャラクター名を「コデックス」から
「ノスティオン」へ変更。実装済みの検索ページ・自己言及ギミック等の表示文言を
更新済み。あわせてP91の応答方式を、インラインテキスト表示から通常の検索結果と
同じクリック可能なカード→専用の隠しページ`pages/glossary/nostion-memory.html`
への遷移に変更済み)

## 今後のタスク候補(`### 13`の隠しページ候補が尽きた場合の予備欄)

**運用ルール(2026-07-27 ユーザー指示)**: このセクションのタスクに着手する際は、
「ページ一覧と進行状況」に新規セクション(`### N. ページ名`)を追加し、通常の
ページと同じように個別タスクとして扱うこと(roadmap-done.mdの「0. 共通パーツ」
配下への記録だけで済ませない)。完了後は他ページと同様に紹介文を追記し、
トップページの更新履歴(お知らせ)に載せること。

**優先順位(2026-07-28 ユーザー指示)**: ROADMAPのタスクが尽きた際、まず
`### 13`(隠し用語集ページ群)の隠しページ候補出しを優先すること。このセクション
(汎用機能の追加提案)は、`### 13`の候補も尽きた場合、または`### 13`が承認待ちで
手が止まっている場合の予備として使う。

1. **学院祭・行事: 次回開催日までのカウントダウン表示**(M) — 各行事カードに
   「次回まであとN日」等の動的表示を追加。日付計算ロジックは`src/logic.js`に
   純粋関数として実装しテストを書く。

(2026-07-28 ユーザー指示により、印刷スタイル拡充・検索キーボードショートカットの
2件は不要と判断し取り下げ。上記1件のみ残す)

## TODO

(現在なし。shop/index.html の noscript フォールバックは対応済み)

## 技術メモ

- **ニュース区切り線の color-mix**: `color-mix(in srgb, var(--brass) 20%, transparent)` は sRGB でブラックと混合するため、インク背景上では実質不可視になる。ニュース項目が実装される際は `oklch` 色空間か 35〜40% に調整すること。
- **エリアページのサムネイル**: 学院内探索の7エリア、購買部の一覧カード(category-card__visual、中央5種+エリア別14種)、学食・喫茶室のエリアタブカード(dining-venue-feature__visual)は全て実画像を反映済み(既存のhero画像を`background-image`で再利用)。CSSグラデーションはフォールバック背景として残置。
- **インタラクティブ要素の方針**: JS は src/ 以下に純粋な関数として実装し、HTML から script タグで読み込む。DOM 操作のみのスクリプトは pages/ に置かない。
- **画像ファイルの黒帯不具合**: 一部の生成画像に、CSSではなくファイル自体に
  焼き込まれた黒帯(letterbox)が混入していたことがある(2026-07-26発覚)。
  新しい画像アセットが届いた際、見た目に違和感があれば「CSS側の問題」と
  即断せず、画像そのもの(上下端が完全な黒でないか)も疑うこと。
